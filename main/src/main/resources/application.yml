server:
  port: 8080
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://huawei.franklin3.top/account-book?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: franklin3
    password: 123456
  data:
    redis:
      host: huawei.franklin3.top
      port: 6379
      password: "@aLZSB666"
      timeout: 50000 # 连接超时时间（毫秒）
      jedis:
        pool: # 连接池配置
          max-active: 8 # 连接池最大连接数（使用负值表示没有限制）
          max-wait: -1 # 连接池最大阻塞等待时间（使用负值表示没有限制）
          max-idle: 8 # 连接池中的最大空闲连接
          min-idle: 0 # 连接池中的最小空闲连接
#  security:
#    user:
#      name: admin
#      password: 666666


mybatis:
  type-aliases-package: com.github.franklinthree.main.pojo
  mapper-locations: classpath:com/github/franklinthree/main/mapper/*.xml

logging:
  config: classpath:logback-spring.xml
